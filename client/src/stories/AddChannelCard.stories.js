import React from "react";

import AddChannelCard from "../components/AddChannelCard";

export default {
  title: "AddChannelCard",
  component: AddChannelCard,
};

const Template = () => <AddChannelCard />;

export const AddChannel = Template.bind({});
